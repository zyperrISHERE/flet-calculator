[project]
name = "flet-calculator"
version = "1.0.0"
description = "Calculator built with Flet"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flet"
]

[tool.flet]
org = "com.zyperr"

[tool.flet.app]
path = "."
